#include "whiteblock2.h"
#include<QTime>
#include<QTimer>

Whiteblock2::Whiteblock2()
{
//    setPixmap(QPixmap(":/play/whiteblock-50px.png"));
//    QTime time=QTime::currentTime();
//    qsrand((uint)time.msec());
//    int x=qrand()%100;
//    int y =qrand() %50;
//    setPos(x+100,y+100);
//    QTimer *timer=new QTimer;
//    timer->start(30);
//    connect(timer,SIGNAL(timeout()),this,SLOT(move()));
//    type=0;
//    setScale(.8);
}


int Whiteblock2::gettype()
{
   // return type;
}

void Whiteblock2::move()
{
   // setPos(x()+.5,y()+2);
}
